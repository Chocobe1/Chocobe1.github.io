/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,

    output: "export",

    compiler: {
        styledComponents: true,
    },
};

module.exports = nextConfig;
